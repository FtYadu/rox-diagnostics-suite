{
  "comment": "GENERATED FILE — the `ecus` map is produced by tools/build-agent-config.ts from data/canonical. Do NOT hand-write ECU addresses, DIDs or routine identifiers here: an invented address silently talks to the wrong controller. Run `npm run build:agent-config` once the canonical extraction exists.",
  "dataChecksum": null,
  "vci": {
    "name": "ROX VCI (DoIP)",
    "serial": "set-me",
    "protocol": "DoIP / CAN FD"
  },
  "tester": {
    "sourceAddress": "0x0E80",
    "functionalAddress": "0xE400"
  },
  "timing": {
    "p2": 100,
    "p2Star": 5000,
    "s3": 5000
  },
  "ecus": {}
}
